【Gemini 主控与独立登录验证】

▶ 用户明确的分工
主要文字交互与需求理解由 Gemini 3.8 Flash 承担；需要生图、视频时由 Gemini 发起对应工具调用。用户自行完成 Google 官方登录。
这是目标架构与登录验证许可，不等于已验证该具体模型在 Gemini CLI Pro 通道可用，也不等于批准直接向外部内测用户共享个人订阅。

▶ 主控与后端职责
用户消息 → Gemini 理解上下文／澄清 → 需要时请求受控创作工具 → 后端校验并建立任务 → 独立执行器调用实际供应商 → 对话展示结果。
• Gemini 决定是否需要工具、提出画面／动作与参数，不直接获得任意服务器命令执行权。
• 工具候选：生成图片、基于选定图片生成视频、查询任务、获取当前用户的指定作品。具体接口与 schema 后续设计。
• 当前图片主服务仍为 Agnes，Google 图片为满足官方接口／费用条件后的备用候选；当前视频任务仍由 Agnes 实际生成。Gemini 主控文本不等于 Gemini 自己使用 Google 的生图／视频会员额度。
• 用户身份由服务端会话提供，不接受模型传入的 user_id 作为权限依据；会话、参考作品和任务均校验归属。
• 无参考图时仍先生成视觉确认稿，用户认可后再提交视频。Gemini 不能跳过这个已确认流程。
• 工具返回真实 task_id／状态／作品信息，不能只把模型“正在生成”的文字当成功。
• 额度预留、防重复、输入版本绑定与任务恢复由后端实现，不能只靠系统提示词约束。
• 角色和输出格式可以通过 Provider 适配层接入；若个人订阅的多用户用途不获支持，则改用具备合适权限的官方 Gemini API 等通路，产品主控分工不需要因此重做。

▶ 独立 CLI 验证入口已准备
已安装官方 @google/gemini-cli 固定版本 0.58.0，位置在代码仓库之外：
/home/kris/.local/share/omniflow-gemini-probe/runtime/
没有修改系统全局 Gemini 安装，也没有安装媒体扩展、接入生产或配置付费密钥。
安装时禁用生命周期脚本并省略可选原生依赖；首次安装下载超时，重试完成。--version 与 --help 已验证可运行；交互式登录仍需用户亲自验证。

入口命令：
```bash
/home/kris/.local/share/omniflow-gemini-probe/login.sh
```
请在用户自己的 SSH 交互终端执行。入口设置 NO_BROWSER=true，使用该已安装 CLI 支持的官方手动授权流程，不尝试在服务器打开桌面浏览器，也不要求公网暴露 OAuth 回调。
• 选择 Sign in with Google，使用 AI Pro 对应账号。
• CLI 显示 Google 官方授权链接，在 Mac 浏览器中打开并授权。
• 如返回授权码，只输入当前 CLI 终端，不贴到本助手会话。
• 登录后先查看 /stats model 与 /model，不发送生成请求；保持 CLI 打开，不选择退出 Google 账号，完成后告知本助手“已登录”，再从另一进程核验授权复用、实际模型与无工具文本调用。

▶ 授权与运行隔离
GEMINI_CLI_HOME、HOME 与 XDG_CONFIG_HOME 指向本验证专用目录；实际 CLI 状态位于：
/home/kris/.local/share/omniflow-gemini-probe/config-home/.gemini/
目录权限仅当前用户可访问；配置和策略文件为 600，登录脚本为 700。认证凭据由官方 CLI 管理，不输出、不复制到仓库或前端。
入口清空继承环境，只保留运行、终端和网络代理所需变量；不继承 Agnes 密钥、其他 Google API key、Cloud 项目或 ADC 配置。强制认证类型 oauth-personal，避免误走按量付费模式。
禁用扩展和 skills，使用 admin policy 全局拒绝所有内置及 MCP 工具，关闭使用统计及遥测。仅配置并静态验证此策略，不宣称已经通过真实模型工具越权测试，也不把同一操作系统用户下的目录隔离说成完整安全沙箱。

▶ 已完成验证
• Gemini CLI --version 返回 0.58.0；--help 包含 model、headless 输出格式、admin-policy 等参数。
• bash -n 登录脚本通过。
• JSON／TOML 配置解析通过，检查了 OAuth 认证锁定、全局 deny 规则及文件权限。
• 无交互终端调用登录入口返回错误并提示用户自行在 SSH 终端执行，没有发起授权。
• 在专用 HOME 与工作目录中 CLI 版本命令可正常运行。

▶ 登录后的验证进展
用户已报告完成登录。本轮启动独立进程验证时，发现当前账号 active 为空、OAuth 凭据文件不存在；最小文本请求在认证阶段以 exit code 41 失败，提示需要交互式重新授权。未生成任何文本、图片或视频。
原因是当前没有可复用授权；为何被清除尚未确认，不能据此断言用户没有登录或模型不可用。请重新授权后保持 CLI 打开，再从另一进程复测。详见 Gemini登录后文本验证记录.md。

▶ 尚未验证
Gemini 3.8 Flash 在 Pro 对应 CLI 通道是否可选、具体标识与可用额度待认证后核验；不把 flash 或 auto 别名当成已经选中 3.8，也不根据模型自报姓名作验证。
需进一步检查官方 CLI 返回的实际模型元数据；如有自动降级，明确报告而非冒称指定模型已成功。
个人订阅可否用于外部内测用户的网站后端仍待确认。当前入口仅为用户本人验证，不开放网页代理。
