# 聚合AI工作台 (AI Media Workbench) - 实现差距分析与待补齐核心功能规划

## 1. 概述与文档审查背景

依据《**AI工作台_产品需求与功能规范_PRD.md**》与《**聚合AI工作台_Google_M3设计方案.html**》的架构设计标准，针对当前前端页面 [`index.html`](file:///home/kris/Codes/AIPannel/index.html) 和后端网关 [`server.py`](file:///home/kris/Codes/AIPannel/server.py) 的落地现状进行了全量技术审查与端到端链路对齐。

### 1.1 当前已有核心资产与已验证能力
1. **Google M3 规范前端界面基座**：[`index.html`](file:///home/kris/Codes/AIPannel/index.html) 已构建高颜值的 Google Material Design 3 风格看板，包含顶部元数据胶囊条、播放器监视器、时间轴波形图、违规卡点列表、意图选择卡片等。
2. **HTTP 206 Partial Content 流媒体服务**：[`server.py`](file:///home/kris/Codes/AIPannel/server.py) 实现了分块流式传输与秒级 Seek 寻道，经 [`test_real_video_pipeline.py`](file:///home/kris/Codes/AIPannel/test_real_video_pipeline.py) 验证已支持音视频精准跳播。
3. **Google AGY (Gemini) 原生大模型穿透集成**：基于宿主机免密凭据，已打通直播文本复盘诊断接口 (`/api/agy/analyze-live`) 与电影级视觉 Prompt 扩写接口 (`/api/agy/compile-prompt`)。
4. **百炼 Wan 视频与生图基础代理通道**：初步接入了 DashScope Wan2.1-i2v 视频合成与 Wanx 生图接口转发。

---

## 2. 现状代码与 PRD/设计规范的差距审查 (Gap Analysis)

| 核心模块 | PRD 与 M3 设计方案要求 | 当前代码现状 (`index.html` + `server.py`) | 差距等级 |
| :--- | :--- | :--- | :--- |
| **音频提取与语音转写 (ASR)** | 支持上传录像/音频，后台通过 FFmpeg 抽取 16kHz 单声道 PCM/WAV，VAD 静音分段，接入 FunASR / SenseVoice 识别，注入电商热词与角色分离 | 前端仅本地挂载播放，转写内容为固化 Mock 数组或手工文本弹窗；后端无音频提取、无 VAD 分段、无真实 ASR 接口 | 🔴 **严重缺失** |
| **失误卡点视频切片 (FunClip)** | 识别违规/卡点后前后缓冲 2 秒，调用 FFmpeg 自动裁剪出 10~30 秒短视频切片，支持看板直接回放与独立下载 | 前端仅弹出 alert 模拟，直接将整段 46 秒原视频重复下载；后端缺少物理切片裁剪接口 | 🔴 **严重缺失** |
| **话术诊断看板与行动项闭环** | 细化 FABE（特性/优势/利益/证据）四维子项得分、开播黄金 3 秒留人评估、下一场 3 项落地行动（Actionable Steps） | 仅呈现综合 FABE 分数（单一数值）与违规列表，缺少 FABE 结构化细分雷达、留人分析与行动建议卡片 | 🟡 **深度不足** |
| **文生图模型智能路由** | 场景一（中文海报排版）路由至阿里开源 Z-Image；场景二（极清商品人像）路由至 FLUX.1 schnell | 后端写死调用 DashScope wanx2.1；缺少 Z-Image 与 FLUX 路由；未配置 Key 时回退为 Unsplash 静态图 | 🟡 **机制未闭环** |
| **Wan 图生视频与素材库沉淀** | 首帧图片公网访问/本地中转，严格执行 Task 状态机，视频生成后归档至素材资产库，支持多端复用 | 本地海报无法转公网 URL 导致百炼报错；轮询无 Key 时走假定时器加载外部 Mixkit 视频；缺乏持久化素材库 | 🟡 **需完善工程链路** |

---

## 3. 接下来需要优先优化补齐的 5 项核心功能点

### 📌 核心功能一：真实音视频预处理与全自动 ASR 转写流水线 (FunASR / SenseVoice)
* **业务背景与用户痛点**：
  目前用户上传真实录像后，看板中的文本无法自动生成，只能使用固化的 5 句样例或人工手打粘贴，无法支撑单场数小时的真实直播复盘需求。
* **具体补齐方案**：
  1. **后端新增音频提取与切片接口** (`/api/audio/extract-and-transcribe`)：
     - 服务端通过 `subprocess` 调用系统 `ffmpeg`，将上传的视频无损抽取并重采样为 `16000Hz, 16-bit, 单声道 WAV`；
     - 引入轻量级能量/静音 VAD 分段逻辑（切片阈值 400ms~800ms），将长音频分段为 30~60 秒的批处理切片，杜绝内存与显存溢出；
  2. **双模 ASR 识别引擎对接**：
     - **云端模式**：接入阿里 DashScope `SenseVoice-v1` 接口，低延时、低成本；
     - **本地模式**：提供向本地 FunASR RESTful 服务 (`http://localhost:8765/asr`) 的派发能力；
     - **电商专属热词注入**：在请求体中动态注入 PRD 规范的行业词（*“拍一发三、破价、上链接、黄车、福利款”*）与用户本场核心 SKU。
  3. **标准时间戳与角色数据对齐**：
     - 自动组装输出包含 `segment_id`, `speaker` (主播/助播), `startSec`, `endSec`, `timeStr`, `text` 的规范 JSON，全量替换前端 `transcriptList`。

---

### 📌 核心功能二：基于 FFmpeg 的毫秒级失误切片与自动化短视频剪辑导出 (FunClip 闭环)
* **业务背景与用户痛点**：
  运营复盘排查出主播违规（如极限词、价格报错）后，核心需求是截取那 10~20 秒的原画切片用于团队复盘通报或主播培训。当前前端直接下载整场原视频，未能实现真切片。
* **具体补齐方案**：
  1. **后端新增物理切片剪辑接口** (`/api/video/cut-clip`)：
     - 接收参数：`source_video`（源文件路径）、`start_sec`（违规起始秒）、`end_sec`（违规结束秒）、`buffer_sec`（默认前后各缓冲 2 秒）、`aspect_ratio`（保持原画或自动裁切 9:16）；
     - 利用 `ffmpeg -ss [start] -to [end] -i [src] -c:v libx264 -c:a aac [output_clip.mp4]` 实现秒级精准音视频切片生成，并保存在服务端 `clips/` 目录；
  2. **前端切片交互升级**：
     - 点击任一违规行或时间轴高亮卡点右侧的“✂️ 导出切片”时，前端发起真实异步剪辑请求；
     - 剪辑完成后弹出 Google M3 浮动播放卡片，支持原地循环回放该 15 秒独立切片，并提供**一键下载 MP4** 与**复制切片公网链接**。

---

### 📌 核心功能三：带货诊断报告看板深度升级 (FABE 四维矩阵 + 3大行动项 + 黄金3秒留人)
* **业务背景与用户痛点**：
  目前大模型诊断仅输出单一的总体得分与简短建议，无法指导主播后续场次如何系统性提升带货话术，缺少量化矩阵与落地抓手。
* **具体补齐方案**：
  1. **强化大模型诊断 Prompt 契约** (基于 PRD 4.1 规范与 DeepSeek/Gemini)：
     - **FABE 结构化细分**：分别输出 **Feature (特性分)**、**Advantage (优势分)**、**Benefit (利益转化分)**、**Evidence (证据支撑分)** 及其得失分剖析；
     - **开场留人评级**：针对前 3 分钟话术输出“黄金 3 秒留人指数”与高唤醒词密度；
     - **落地行动指南 (Actionable Steps)**：输出 3 项下一场必须立即执行的标准化改写口令。
  2. **前端 M3 看板组件扩充**：
     - 增加 **FABE 战力雷达/进度条矩阵**，直观呈现产品卖点 vs 催单话术的失衡点；
     - 增加 **《下场直播 3 大落地整改卡》**，提供一键复制话术对比卡；
     - 时间轴增加开播前 3 分钟的黄色留人钩子专区，与视频播放实时联动。

---

### 📌 核心功能四：文生图多模型智能路由 (Z-Image 中文排版 vs FLUX.1 极致逼真)
* **业务背景与用户痛点**：
  用户在勾选“电商带货海报”并填写中文印字时，普通文生图模型容易产生不可读的乱码字符；而生成人物样照时则需要极致的光影质感，单一模型无法兼顾。
* **具体补齐方案**：
  1. **后端构建模型派发路由** (`/api/generate-image` 升级)：
     - **路由分支 A (含中文排版)**：当前端表单中 `hasText === true` 且提供 `textContent` 时，自动派发至 **阿里开源 Z-Image** 接口（或专门针对中文文字渲染优化的模型服务）；
     - **路由分支 B (极致光影商品/人像)**：当为纯商品构图、棚拍或人物时，派发至 **FLUX.1 [schnell]** 或 DashScope 图像渲染引擎；
  2. **Prompt 编译器输出分流**：
     - Gemini 编译接口按 PRD 4.2 规范同时输出：`English Prompt` (用于底层生图)、`Chinese Typography Prompt` (用于中文文字版式控制) 和 `Negative Prompt` (负向提示词过滤伪影)；
  3. **生成图片本地静态持久化**：
     - 生成的图片统一落地保存到服务端的 `generated_images/` 目录，对外提供本服务可直读的 HTTP URL，为后续 Wan 图生视频提供可靠的首帧输入。

---

### 📌 核心功能五：通义万相 Wan 图生视频全状态机调度与本地素材库资产沉淀
* **业务背景与用户痛点**：
  阿里百炼 Wan2.1 图生视频接口要求首帧图片必须是公网可访问的 URL，目前传入本地图片会直接报错；且缺乏完整的生命周期状态管理与历史资产复用能力。
* **具体补齐方案**：
  1. **首帧图片中转与直链服务**：
     - 在发起 Wan 视频生成前，若首帧为本地截帧、本地上传或本地生成图，后端自动将其托管并暴露公网临时地址（或对接 OSS/TOS，或通过内网直通代理）；
  2. **异步状态机严谨管理**：
     - 完整实现 PRD 3.4.2 状态机：`SUBMITTED -> PENDING -> RUNNING -> SUCCEEDED / FAILED`；
     - 前端按 3 秒间隔轮询 `/api/wan-video/status/<task_id>`，展示细粒度渲染百分比，若触发内容合规或 GPU 超时等错误，输出中文人话排查指引与一键重试；
  3. **轻量素材资产库 (Asset Library) 沉淀**：
     - 在主界面或独立抽屉建立“场次素材归档库”；
     - 每一场直播的分析报告 (Markdown/JSON)、切片短视频 (MP4)、生成的商品海报 (PNG) 及万相合成视频 (MP4) 自动落盘归档，支持一键打包导出并持久化留存。

---

## 4. 实施优先级与建议排期

| 优先级 | 功能模块 | 预期工作量 | 核心收益 |
| :---: | :--- | :---: | :--- |
| **P0** | **功能二：FFmpeg 真实毫秒级切片与导出** | 0.5 天 | 彻底解决当前切片“假下载”问题，直接交付运营可用的取证工具 |
| **P0** | **功能一：FFmpeg 音频提取 + ASR 真实转写对接** | 1 天 | 终结纯 Mock 假数据，实现“导入任意录像直接出字幕”的核心主流程 |
| **P1** | **功能三：FABE 四维矩阵与 3 大整改行动项看板** | 0.5 天 | 契合 PRD 规范，极大增强大模型复盘的专业说服力与落地价值 |
| **P1** | **功能五：Wan 首帧直链中转与素材资产库持久化** | 1 天 | 跑通图生短视频真实生成闭环，沉淀复盘历史数据 |
| **P2** | **功能四：文生图 Z-Image 中文排版模型路由** | 1 天 | 提升中文海报排版不乱码能力与多模型灵活性 |

---
*本规划文档已保存至项目根目录，可作为后续编码实施与功能验证的检查基准。*
