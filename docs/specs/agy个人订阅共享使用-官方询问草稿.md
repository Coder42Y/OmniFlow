【个人 Pro 订阅通过官方 agy CLI 服务受邀用户：询问草稿】

状态：暂停使用。用户随后决定不把官方共享许可确认作为当前受控测试前置；本草稿仅供未来需要时参考，未发送、未获官方答复，不自动联系官方。内容不含账号、域名、OAuth token 或客户资料。
官方提问入口依据：https://antigravity.google/terms 指向 https://discuss.ai.google.dev/c/antigravity/64；也可从 https://antigravity.google/support 查找当前官方支持渠道。需要能代表提供方解释许可的答复，普通社区成员的回复不视为授权。

▶ 建议询问正文
您好，我希望确认下面这种使用方式是否被 Google Antigravity 个人 Google AI Pro 订阅的条款允许：

1. 我在自己管理的服务器上安装并直接运行官方 Antigravity CLI（agy）。
2. 由我本人通过官方登录流程授权，授权缓存始终交由官方 CLI 管理；不提取、复制或导出 OAuth token 给第三方模型客户端，不直接请求 Antigravity 内部接口。
3. 我自己的网页应用通过官方文档所示 subprocess / stdin / stdout 调用 CLI，使用 --input-format stream-json 与 --output-format stream-json 保持多轮会话。
4. 网页面向最多10名外部受邀测试用户；每人使用网页自己的账号，每个创作会话有独立 CLI 进程／对话 ID，但均使用我的同一份个人 Pro 订阅授权。
5. CLI 只调用我配置的受限创作工具，不向网页用户暴露终端、文件系统、Google 账号或其他个人资料，不绕过提供方用量限制。
6. CLI 或管理进程可能由 systemd / Docker 长期托管。我们不会把自动化支持当成多人共享许可，因此先询问再开放服务。

官方 headless 文档明确支持程序驱动 CLI 和单进程多轮对话；同时附加条款禁止第三方软件、工具或服务访问 Antigravity。请明确：
• 上述“直接运行官方 CLI”的受邀网页服务是否允许使用个人 Pro 订阅？
• 若只限我本人使用是否允许；允许本人自动化是否也覆盖上述外部用户请求？
• 免费内测与收费服务在许可上是否有区别？
• 如果不允许，是否必须每位用户各自取得许可，或改用 Enterprise／API 方案？请指出适用条款和支持的正式路径。
• 是否有额外的数据告知、区域、年龄或超额费用配置要求？

请提供明确的官方解释或相应条款链接。在收到说明之前，我们不会把这种模式宣称为已经得到官方的多人共享许可确认。

谢谢。
