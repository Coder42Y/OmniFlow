【完整新版发布实施计划 v1】

▶ 用户最新优先级：14:43视频先行
用户在选择A之后进一步要求“先部署上去我用用这个视频生成吧”。因此先交付可真实生成视频的新版入口，其余功能随后补齐；不再要求文本、生图全部完成才允许视频试用。视频不得用假提供方或本地运镜冒充AI，未启用功能如实显示不可用。这不同于此前拒绝的“全部AI禁用基础版”。
当前09／10应优先支持媒体独立运行配置：仅启用ai_video时，不要求提供未启用的agy镜像、Google授权、文字worker或相关账单证明；只保留视频自身的真实授权、免费条件、限流、任务持久化与隔离/文件安全。其他模型不可因视频通过而被默认为已核验。既有完整模式保留并继续实现，不新增另一套绕过任务服务的临时视频接口。
用户已完成服务器Vercel官方登录，主控whoami及原项目project inspect均成功，发布账号权限不再是待办。

▶ 用户决定与权限
2026-09-09 13:25用户批准发布，13:31选择A，随后按上述要求调整为真实视频先行。整体发布许可已经取得，不重复询问A/B或整体是否允许上线。
未取消的边界：不收费回退、不新增云资源、不丢旧数据、不使用测试账号／假生成冒充真实服务、不擅改Google登录、无备份承诺。已取消视觉模型／外观复审，只保留代码布局与功能、安全验证。
正常接线、配置生成、测试及修复属于本次实现，不需要逐项找用户确认。真实管理员密码由用户在终端隐藏设置；不在聊天、命令参数或日志收集密码。

▶ 为什么还需追加实现
现有00–08是受控离线开发与假提供方验收。真实适配器存在，但生产CLI还未将它们接入API／文字管理器／媒体worker；Settings只有disabled/mock；AgyProvider缺省launcher=None。完整发布不能靠把环境改成production或打开几个本地开关就假装完成。
本轮追加09、10两个有限开发阶段，随后才执行真实生产验证与域名切换。09、10的编码agent仍在当前worktree，测试仍使用临时数据与替身，不读取旧.env／真实凭据、不操作生产、不发真实生成；实现出的生产代码必须走真实运行路径，替身只用于验证同一路径，不允许靠导入tests上线。
公开官方文档可只读查证；不得假设CLI协议或权限能力存在。不能因为无法在离线环境取得真实账号证据就省略生产接线，能编码的先完成；真正需要账号持有者核验的具体项目单列，不编造证据。

【09-runtime：真实生产入口与进程接线】

1. 阅读实际cli.py、app.py、RunManager、TaskWorker、AgyProvider、ToolGateway、EvidenceGate及配置。按需核对本项目已保存的真实研究报告agy官方常驻调用与多人使用条款核对.md、Gemini真实调用生图与视频工具实测.md、Gemini主控决策验证记录.md；不要只依据fake_agy.py反向猜测官方协议，也不要读取真实授权文件或其他私有会话。不重写已经通过的业务层；解决真实运行入口缺失，不只新增一份方案。
2. 提供显式生产运行装配，使API能力/任务受理、文字管理器、媒体worker使用一致的运行配置与真实适配器。默认继续拒绝未配置状态，但有受控配置后应可运行，不永远返回PROVIDER_UNAVAILABLE；增加真实worker就绪与过期判断，不能用Web进程live冒充模型可用。
3. 官方agy可信启动器使用固定gemini-3.8-flash-low、每用户/每对话独立目录与CLI上下文、旧会话按精确ID恢复。落实最小工具权限、最小授权材料可见范围和子进程边界；不能只声明--sandbox就当作已隔离。根据官方协议校验真实可支持的参数与输出，不能要求官方从未承诺返回的测试专用字段。
4. 使用既有官方登录的受控生产路径，不能要求模型读取或复制用户全部授权目录，不把令牌、原站.env内容或账号秘密放进提示、argv、日志或普通API。测试中使用临时合成授权材料及假可执行文件走同一装配入口；不在本轮读取真实凭据。
5. Agnes HTTP、结果下载与FFmpeg接入真实worker；密钥仅由显式受保护的生产运行配置提供，日志脱敏，不自动扫描旧.env。服务端继续校验首帧、不可变版本、归属、幂等及提交未知。禁止在生产导入backend/tests下的任何假提供方。
6. 提供可信条件核验接入与可验证的失败原因。费用/超额关闭、供应商429及隔离条件不得靠随手设置free=true/isolation=true放行。不能自己更新证据时间让过期记录永远有效；能自动核验的实现只读核验，无法官方自动核验的指出具体缺项与安全人工流程，不反复讨论已明确非测试阻断的共享条款。
7. 新增真实启动路径的替身进程/HTTP回归，覆盖默认拒绝、正确装配、隔离、跨进程一致性、证据过期、费用状态未知拒绝、错误协议、退出及重启。所有已有功能/安全测试保留；完整记录实际实现与尚需现场核验的差别。

【10-release：发布工具与全量代码验收】

1. 基于09成果生成可审查的生产启动、环境配置模板、用户systemd单元与发布/回退工具，放在backend/deploy等worktree路径；本阶段不安装到真实服务、不直接执行切换。补齐真实前端dist与API同源交付，不能复制测试e2e_server当生产入口。
2. 生产静态资源只提供批准的dist资源，不暴露仓库、数据、凭据、测试入口或任意目录。新数据必须使用独立持久路径，不把pytest/e2e临时目录当生产库，旧站源文件和数据原地保留。
3. 识别现有Vercel静态前端→Cloudflare隧道→本机的分层入口。后续沿用既有机器/域名/资源，不擅自创建新域名、云服务或付费方案。仅重启本机后端不能算新前端已经发布。
4. 准备发布前检查：实际版本摘要、真实运行身份、目录权限、管理员初始化、生产拒绝mock/调试入口、Host/Origin/Secure Cookie/CSRF、代理SSE与HEAD/Range。代理头只在明确可信边界处理，不盲信客户端自报头。
5. 准备最小真实调用验收工具与记录格式：明确运行来源、模型/任务ID、实际产物、重放次数与真实媒体元数据；无真实成功就不报通过。执行前确认无付费回退/超额扣费；供应商限制即停，不无限重试、不自动升级方案。该阶段仅用替身测试这些工具，不自行调用真实生成。
6. 生产切换必须检查前置验收及真实运行证据，不能提供绕过所有安全条件的--force。保留旧版本和入口回退路径，回退不抹除新版产生的数据，不删除客户素材，也不建立用户已拒绝的备份策略。
7. 独立reviewer复核09/10新代码及全量回归、包中无测试依赖运行入口/秘密、生产默认值与回退边界；不做视觉模型复审。

▶ 阶段完成之后
09/10离线通过只表示生产接线代码与发布工具已验收，不代表真实供应商或正式域名已完成切换。主控随后按本次已获许可开展现场核验和实际发布；若管理员初始化、真实费用状态或既有部署账号权限需要用户参与，只提具体缺项，不重复整体发布授权。
视频真实接入与必要验收完成之前，旧站继续提供服务；通过后可按最新要求先发布视频可用版，其余未就绪功能明确禁用并继续补齐。不得把无法真实生成视频的版本称作视频试用版。
