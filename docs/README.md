【创作工作台文档入口】

▶ 当前线上状态（2026-09-09更新）
当前版本r-20260909-165903已按用户要求将原永久邀请改为可重复注册普通账号，仅作用于指定链接，仍可撤销；其他邀请默认一次性。保留中文用户名说明及6–128位密码。见releases/原邀请改为重复注册-20260909.md、releases/永久普通用户邀请-20260909.md。
新版页面与新FastAPI已部署到 https://ai.pannel.kris42y.tech ，正式域名HTTP和Chrome页面／无效登录流程检查通过。真实AI生成仍未启用；kris管理员已按用户明确要求创建，密码不记录在文档中。本次仅调整邀请复用规则，没有更改现有账号或密码。后台通用开发循环仍按此前指令暂停。本次没有回滚。
实际入口、服务、验证与限制见 releases/新版域名切换记录-20260909.md。此记录取代下方历史文档中“尚未发布”的现状描述。

▶ 当前设计基线
specs/创作工作台重构-Spec-v1.0.md
已确定Vue 3＋Vite、Python＋FastAPI、SQLite、独立worker与systemd；每次新建创作对话独立CLI上下文；不设业务配额、暂不备份、沿用现有域名。
少量客户体验默认在Spec第十二部分集中列出，发布前复核，不冒称此前逐项获批。

▶ FastAPI接口第一版
• api/FastAPI接口文档-v1.md：接口行为、权限、幂等、事件恢复、文件与错误处理。
• api/路由索引-v1.md：逐操作路由、请求／响应类型和鉴权要求，自动生成。
• api/openapi-v1.json：OpenAPI3.1.0契约，可导入Swagger Editor／Apifox等工具；尚未部署。
• api/build_openapi.py：重建OpenAPI与索引，使用Python标准库。
• api/test_contract.py：离线schema、示例、路由及安全声明检查，依赖见api/requirements-docs.txt。
• api/接口文档校验记录-v1.md：本轮实际验证结果和未验证边界。

▶ 已批准的界面原型
specs/assets/codex-wave-rail.approved.html
资源说明：specs/assets/README.md。快照不得覆盖改版，原始已批准文件保持不变。

▶ UI规范与真实截图审查
• design/design.html：可独立打开的UI设计规范及组件样册。
• design/README.md：规范维护、测试与只读预览说明。
• design/前端视觉整改与验收-v1.md：历史视觉建议，强制视觉验收已于2026-09-09 11:53按用户要求撤销。
• design/视觉模型审查-快照v1.md：实际8张图像输入的独立审查结果，不是纯CSS检查。
应用仍由后台pi实现；后续仅做代码侧对齐排版、CSS媒体查询、布局功能与安全检查，不再做模型读图／外观复审。样册通过不等于应用完成。

▶ 完整新版发布（已获授权，用户已选A）
• plans/完整新版发布实施计划-v1.md：生产运行接线、发布工具及真实验证边界。
• releases/新版发布预检-20260909.md：实际缺口、现有入口与发布条件。
基础开发正常通过后接续09／10；不先发布AI不可用的基础版，不把代码／假提供方验收当作已经上线。

▶ 历史与研究
Spec v0.1、v0.2保留历史，不把其中已撤回的日配额、存储上限、备份建议当作当前要求。
更早讨论与实测报告位于项目根目录，最新确认汇总见 ../创作工作台重构续议.md。
官方询问草稿目前暂停，未发送；不作为当前受控测试前置。

▶ 范围提醒
接口契约测试不启动FastAPI、不接真实账号数据库、不调用生成服务。后台实现与图像审查有各自的进度和证据，不发布网站。接口结构验证或视觉样册通过均不等于真实应用的全量功能／安全验收完成。
