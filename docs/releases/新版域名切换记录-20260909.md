【新版域名切换记录：2026-09-09 15:37】

▶ 最新用户要求与结果
用户明确要求尽快把新版代码部署到现有域名、确保前后端正常工作，并禁止回滚。本轮已完成实际部署和域名验证，没有执行回滚；先前暂停的后台编码仍保持暂停，没有顺带恢复其他开发任务。
正式入口：https://ai.pannel.kris42y.tech
当前版本：r-20260909-151201
Vercel部署：dpl_K7kt1tifMUN8XjGjmRBD2hULXr2y
版本地址：https://omniflow-r329nyg3w-mj53why-2182s-projects.vercel.app
注意：新版页面和基础API已发布，但真实AI视频／聊天／生图未启用，不是完整生成能力交付。

▶ 上次卡点与本次修复
之前从原工作目录调用CLI，即使带--cwd上传了新目录文件，实际部署记录仍采用旧vercel.json：builds只有index.html和prototypes/mobile.html。因此新路径被旧路由返回为首页。
此前发现预览地址跳Vercel登录也是事实，但将其视为唯一原因不完整。此次通过Vercel API读取实际部署配置，明确发现旧构建入口；改为subprocess实际cwd指向新site，另显式指定该目录vercel.json。
再次查询Vercel部署配置，确认实际builds为index.html、404.html、release.json、assets/**；随后受保护部署直读release.json返回正确版本，而不是HTML。正式promote后，公开主域名也返回正确版本。未关闭Vercel项目访问保护。

▶ 部署结构
• 冻结发布副本：/home/kris/.local/share/omniflow-production/releases/r-20260909-151201，源摘要位于source-manifest.json，55个文件。生产不直接运行持续改动的worktree。
• 独立运行环境：上述版本/.venv，从锁定依赖离线安装22个生产包，没有安装dev依赖或更改全局Python／Node。
• 私有数据：/home/kris/.local/share/omniflow-production/data，新建独立SQLite，迁移版本8；未导入或清空原站数据，无备份承诺。
• 私有配置：/home/kris/.local/share/omniflow-production/production.env。production模式、真实生成暂disabled、无mock回退、保留Secure Cookie及CSRF。
• 现有Cloudflare隧道仍连接127.0.0.1:8080；固定Node网关将/api/v1转发到新API 8765，其余已允许的旧API／媒体路径转到保留旧代码的回环8082。只转发固定本机目标，不公开文件目录。
• 原aipannel.service已停止并取消开机启用，源文件和配置仍保留。新增omniflow-api／text／media／legacy／gateway五个用户服务均active且enabled。旧代码通过仅改变绑定地址的包装器继续提供兼容API，不改原server.py。
• Vercel正式提升会更新同项目别名；备用别名重定向到主域名，避免多个Origin／Cookie入口混用。未修改DNS、未重启隧道、未创建新云资源。
• 原临时隧道的重启URL变化风险仍存在，本次未将其冒称稳定命名隧道。

▶ 实际正式域名检查
HTTP检查：
• / 与 /release.json：200，首页字节摘要与冻结新构建一致，版本号正确。
• /api/v1/health/live、/api/v1/health/ready：200。
• /api/v1/auth/policy、/api/v1/auth/csrf：200，保留Secure／HttpOnly Cookie。
• /api/health：200，旧API兼容仍可达。
• /.env：404；未登录访问/api/v1/admin/users：401。

真实Chrome浏览器检查（不是视觉复审）：
• 1440／390／320px均实际打开正式域名，标题为OmniFlow · 创作工作台。
• JS／CSS资源正常加载，0个页面脚本错误，无整页横向溢出。
• 浏览器实际Cookie为Secure／HttpOnly／SameSite=Lax。
• 通过真实登录表单提交明确不存在的用户名，收到401，证明新前端经生产域名请求到新鉴权接口；没有创建测试账号、没有发送真实生成。
• 证据：.superpowers/releases/immediate-20260909/browser-checks.json及state.json。

▶ 尚未完成，不冒称通过
• 当前管理员数量0。没有默认账号／密码；成功登录及登录后的生产操作尚未验证。用户需要在服务器终端执行下述命令，自行隐藏输入12–128字符密码两次：
```bash
~/.local/share/omniflow-production/create-admin kris
```
kris是可替换的用户名示例。不要把密码发在聊天里。注册仍需要管理员签发邀请。
• 真实视频、生图、文本目前未启用；没有用测试媒体冒充真实输出，普通后端就绪不表示供应商就绪。
• 当前只验证部署、匿名页面、无效登录拒绝与入口安全，不把它说成成功登录后全业务或真实生成已经通过。

▶ 不回滚与现场保留
本轮状态已设置automatic_rollback_authorized=false，旧rollback.py增加拒绝执行保护。之后如有问题只报告并向前修复，不自动回滚。
之前两次回退发生在用户最新禁止回滚之前，保留日志说明过程；本轮没有再次回退。
Vercel官方curl诊断曾生成项目预览保护绕过凭据，未打印其值或向用户分发；项目保护未关闭，.vercel目录不在静态构建入口中。未将登录凭据打进公开资源。
