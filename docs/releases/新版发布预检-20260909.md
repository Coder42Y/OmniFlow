【新版发布授权与预检：2026-09-09 13:25起】

▶ 已获授权
用户明确说“可以发布新版了”。整体发布许可已确认，不再将“尚未获得发布授权”作为阻断，也不重复索取同一许可。
发布许可不代表允许丢弃旧数据、关闭鉴权、伪装AI可用、使用测试账号／提供方上生产、开启付费回退或新增云资源。
本次仅执行只读预检，尚未修改生产服务、域名、Vercel、隧道或生产数据。

▶ 当前开发状态
控制器已接受00–06阶段；06独立复审证据为前端81项、后端675项及专项通过。13:25检查时07-e2e仍在编码修复轮，08最终代码／安全审查未完成。
07编码报告记录后端797、前端81、端到端5项通过，但报告自述不是独立审查，不能据此宣布可以生产发布。其CLI／媒体提供方为专门的测试替身。
用户已取消视觉模型与外观复审，不将这些重新纳入发布条件。

▶ 实际代码发现的核心缺口
• backend/src/omniflow/cli.py：生产入口的run-manager直接构造RunManager(database)，media-worker直接构造TaskWorker(database)，没有装配真实提供方。
• backend/src/omniflow/agy_adapter.py：AgyProvider默认launcher=None，没有可信启动器时拒绝启动。不是简单把环境变量改成production就能真实聊天。
• backend/src/omniflow/config.py：provider_mode只有disabled／mock，production拒绝mock。
• backend/README.md及07报告：真实agy启动、授权／费用证据采集、宿主隔离及真实Agnes接线尚未完成；就绪探针不能证明模型可用。
结论：适配器代码和测试闭环已有，但完整生产生成链尚未接通。若此刻直接切换，只能上线账号／对话记录／上传／作品管理等基础功能；不能声称真实聊天、生图和视频可用，也不能用测试替身冒充。
该问题不同于发布许可，也不只是视觉或格式问题。

▶ 现有发布入口
• 原aipannel.service正在运行，工作目录为/home/kris/Codes/AIPannel；独立aipannel-tunnel.service同样运行。未停止或修改它们。
• 仓库vercel.json显示：页面静态资源在Vercel，/api等经Cloudflare临时隧道转发。仅替换本机后端不会自动发布新的Vue页面；还须核对实际远端发布配置，不能把仓库配置直接当成线上配置已完全一致的证明。
• 当前PATH未找到Vercel CLI，也没有查询／导出它的凭据。后续部署需核对既有发布授权和渠道，不新增付费资源。
• 新后端无默认管理员，create-admin要求终端隐藏输入密码；不得拿合成测试账号当真实管理员，也不要求用户把密码贴进聊天。

▶ 切换前必要条件
1. 07联调及08独立代码／安全验收完成，冻结所发布版本，不从正在改动的工作树直接提供生产服务。
2. 发布功能范围已选A完整可生成版：先补生产运行接线、验证真实调用与相关安全／费用条件；不再考虑AI禁用的基础版替换。
3. 新服务使用独立私有持久目录，旧源码与数据原地保留；不自动迁移旧匿名记录、不删除素材、不设置备份或收费兜底。
4. 安全完成真实管理员初始化。既有域名下验证HTTPS、Cookie、CSRF、SSE与受保护文件；保留旧版本及入口配置回退路径，回退不抹除新产生数据。
5. 切换后重新从正式域名验证真实状态；失败按预定入口回退。不得把HTTP 200或测试提供方通过冒称完整业务验收。

▶ 用户选择与当前处理：13:31更新
用户已明确选择A：补齐真实AI接入后发布完整新版。不发布AI禁用的基础版，不重复询问整体授权或A/B选择。
继续既有07／08代码流程，不并行争改backend／frontend，不提前中断旧站。追加09-runtime与10-release的受控生产接线／发布工具开发，具体范围见docs/plans/完整新版发布实施计划-v1.md。离线实现验收后再进行真实接入核验和域名切换，不把代码完成冒称已经上线。
13:43检查：原控制器已推进到08-audit，未被主控打断；coding:release-handoff处于waiting，只在00–08正常完成后一次性接续09/10。当前不是09已经开始，更不是生产已切换。接力器及调度／监看合计28项本地测试通过。

▶ 14:37验证进度
08基础阶段已独立审查通过：亲测后端809项、前端82项、端到端5项；只表示离线基础代码验收，未调用视觉模型。
接力器14:09正常续接09-runtime，当前仍在编码／交接报告阶段，后台活跃、未暂停。09新增代码最新回归为后端848 passed in 197.72s、前端82 passed、端到端5 passed，构建／格式／契约通过；09尚未独立验收。
真实聊天、生图、视频与正式域名生产验证仍未完成，不能把这些测试替身的成功当作完整发布验证。尚未切换旧站。
