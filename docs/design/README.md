【OmniFlow UI规范与历史视觉审查】

▶ 当前范围（2026-09-09 11:53更新）
用户取消后续视觉模型／审美复审。样册、tokens和已有组件保留，以下视觉报告只供历史参考，不再要求逐项外观整改或模型读图通过。开发仅继续组件与文字对齐排版、CSS媒体查询、响应式和功能／安全检查；最新验收以实施计划和docs/progress/06-ui-代码收尾.md为准。

▶ 先打开哪个文件
• design.html：可直接双击打开的完整单页样册，CSS、SVG和交互全部内嵌，无外部字体／脚本／统计／API请求。不是已经上线的产品页面。
• 前端视觉整改与验收-v1.md：历史视觉整改意见；其中强制视觉验收现已撤销。
• 视觉模型审查-快照v1.md：独立图像模型针对实际应用截图的原始意见；主控采纳和纠偏见整改文件，不能照单新增后端状态。

▶ 样册覆盖
颜色与语义token、对比度、文字层级、间距／圆角、按钮与输入各状态、统一SVG图标、独立侧栏／对话布局、批准横线波峰、媒体版本／任务真实状态、空／加载／错误／不可用、弹窗和320／390px响应式、前端视觉验收清单。
用户说“类似Claude design.html”按独立设计手册的形式落实，不复制Claude品牌、暖色配色或改变已有交互方向。

▶ 维护方式
编辑tokens.css及design.template.html，然后：
```bash
python3 docs/design/build_design.py
python3 docs/design/test_design.py -v
node docs/design/tests/browser-smoke.cjs
```
浏览器脚本使用本项目frontend/node_modules/playwright与现有/usr/bin/google-chrome；从项目根目录执行，不安装全局依赖、不调用生成服务。截图和证据写入被忽略的.superpowers/design-tests/。
独立样册必须与tokens来源一致；不要仅改生成文件。应用接入由当前编码agent负责，主控没有并行改frontend业务代码。

▶ 已有真实证据
1. 冻结当前前端26个文件，记录SHA256，在副本构建并使用Chrome截图12张，覆盖批准原型、桌面展开／收起／波峰、390手机、320精修、登录、视频、空态与作品库。该快照无JS错误、记录时无整页横向溢出，截图使用合成API数据及既有合成样片。
2. 独立pi视觉审查：固定openai-codex/gpt-6-astra，实际8个image附件，只有read工具，正常最终响应及退出码0。结果有条件通过、5项P2和3项P3；这不是当前应用的最终验收。
3. 主控观察样册桌面／控件／手机／320弹窗截图。静态4项测试和三个视口浏览器测试通过：对比度、无溢出、标题／操作区可见、Tab循环、Escape返回、波峰键盘、44px按钮及无外部请求。
4. 原批准快照未改，波峰6/10/28px几何保留；真实应用的后续视觉复审已按用户最新要求取消；功能／布局代码检查仍保留。

本次快照、截图、模型事件及调用证据位于.superpowers/ui-audit-20260909/，仅本机私有保存，不包含在样册HTTP预览中。原始模型报告自述“未保存文件”是只读审查员当时的行为，报告由主控随后提取保存。

▶ 只读预览
preview.py只服务/和/design.html，不公开目录、源码、审查日志或业务接口；只允许回环/RFC1918内网IP，最多运行1小时。临时预览不修改生产域名、DNS或现有站点。
本机预览示例：
```bash
python3 docs/design/preview.py --bind 127.0.0.1 --port 4178 --info .superpowers/design-preview.json
```
这条localhost示例仅在服务器本机可直接访问；Mac远程查看需要内网可达地址或SSH端口转发，不把它冒充Mac可直接打开的链接。
