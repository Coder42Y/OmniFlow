【07-e2e：真实新 API 与假提供方浏览器联调】

【尝试 2：修复独立审查指出的媒体恢复测试竞争】

▶ 当前结论（2026-09-09 13:31）
• 已修复上轮独立审查发现的唯一阻断项，专项及本轮固定全量全部通过：后端 797、前端 81、E2E 5；E2E 前置生产构建通过。编码结果可交外层独立复审，不冒称本轮独立审查已经通过。
• 完整阅读实施计划、Spec v1.0、FastAPI 接口文档、当前阶段记录、直接依赖的代码收尾记录与指定上轮完整审查报告；核对实际 App.connect、MediaCard、测试路由、真实 API 联调入口和契约差异测试。未重建已通过阶段、扩展功能或修改设计契约。
• 上轮编码者曾得到前端 81 passed，但独立审查两次全量均 80 passed／1 failed；不能用历史偶然通过抵消审查失败。以下历史记录保留，当前状态以本节新证据为准。

▶ 缺陷与确定性修复
• 原用例在「实时连接」出现后立即取消 brokenMedia 故障。App.connect 会清空消息和版本，媒体卡片重新挂载；连接状态只代表事件订阅，不保证新图片请求已经失败。较慢的图片请求会在故障解除后直接成功，随后等待「重试读取」必然超时。
• 仅修改 frontend/tests/layout.test.js：合成媒体路由增加可选 holdMedia Promise；原恢复用例在点击重新连接前挂起媒体响应，明确等待第二次内容请求、实时连接与新图片节点，并断言此时没有重试按钮。此时图片仍未完成，确定覆盖上轮暴露的慢媒体顺序，不依赖固定 sleep 或机器负载。
• 释放等待时继续保持故障，待新卡片真正显示「重试读取」后才解除故障并点击。保留原有图片 naturalWidth=360、无 POST 生成请求、两次事件订阅、首帧确认和无页面异常断言；另增加恰好三次内容读取断言（初始失败、重连失败、手动重试成功）。
• finally 无条件释放本轮等待再关闭 context，失败路径不将合成媒体响应永久挂起。未增加超时、删测试、放宽预期或修改产品组件。

▶ 本轮实际测试命令与结果
```bash
npm --prefix frontend run test -- --run tests/layout.test.js -t '媒体读取失败可恢复'
.venv/bin/python -m pytest backend/tests -q
npm --prefix frontend run test -- --run
npm --prefix frontend run test:e2e
npm --prefix frontend run format:check
.venv/bin/ruff check backend
.venv/bin/ruff format --check backend
.venv/bin/python docs/api/test_contract.py
git diff --check
git diff --exit-code -- server.py index.html requirements.txt deploy.sh \
  docker-compose.yml Dockerfile nginx.conf vercel.json start.sh manage.sh
sha256sum docs/specs/assets/codex-wave-rail.approved.html
```
• 专项：1 passed、27 skipped，1.94s；跳过只因名称筛选，随后固定全量无跳过。
• 后端：797 passed in 179.71s，包含 app.openapi 与设计 64 个 schema、51 个操作及安全声明差异回归。
• 前端：5 文件、81 passed，18.77s；本轮只跑一次专项与一次固定全量，没有连续重跑直到碰巧通过。
• E2E：5 passed、0 failed／skipped，11.41s；pretest:e2e 实际执行 npm run build，Vite 24 modules、801ms，产物 JS/CSS 摘要名称与上轮一致。新 FastAPI、真实浏览器、独立假提供方的邀请注册、上传／生图／固定版本视频、关闭页面与 worker 重启、下载／HEAD／Range、A/B 隔离、精修版本、提交未知和注销撤销全部通过。
• Prettier、Ruff、85 个 Python 文件格式、空白及列明旧站／部署文件差异检查全部通过。文档契约检查：40 路径／51 操作／64 schema／525 引用、22 契约示例／8 文档 JSON／1 SSE／22 类拒绝用例通过；未提供官方元 schema，不宣称其完整验证。
• 批准原型 SHA256 仍为 be8f15d9510bfb6a2bd937409c142fa7473876c17f901a7e495228ece1189448。没有安装依赖或更改锁文件。
• 本轮完整测试日志：backend/var/07-attempt2-targeted.log、07-attempt2-backend.log、07-attempt2-frontend.log、07-attempt2-e2e.log（本地忽略目录）。

▶ 代码／安全自查与清理
• 变更只在测试路由等待和原恢复用例；正常未设置 holdMedia 的夹具不受影响。故障在服务读取前保持、错误按钮对应重新挂载后的卡片，恢复按钮真实发起媒体 GET，不新建生成任务。
• 重新核对 E2E 服务仅绑定 127.0.0.1 随机端口、临时数据库、父子 stdin 控制、明确假 CLI 启动器、worker 网络拒绝与假提供方账本；浏览器未替换业务 API，Secure／HttpOnly Cookie 和 CSRF 保持。测试注入不是完整宿主隔离证明。
• 本轮测试正常退出；13:30 按当前 worktree 的 e2e_server／fake_agy／Vite／Vitest 路径检查无匹配进程，backend/var 无 e2e-* 临时目录。没有终止其他会话／进程。
• 未调用视觉模型或读取 PNG 做审美检查；仅保留既有测试自动诊断截图。保留 320／390px、桌面、弹窗／键盘与溢出功能回归。

▶ 修改文件与接续
• 本轮手工修改仅 frontend/tests/layout.test.js、docs/progress/07-e2e.md；其他先前未提交文档／代码均保留，原站、部署、实施计划、控制器及控制状态未改。
• 外层独立审查需复核本次 Promise 等待及失败清理、固定全量结果；通过后再进入阶段 08，本轮没有另起 agent 或越过阶段。
• 真实 agy／Agnes、免费与费用开关、完整宿主隔离、真实质量、长时运行、代理 SSE、Mac／iOS 真机及生产发布仍「未完成／未验证」。没有真实供应商调用、生产库操作、Google 授权修改或收费回退；产品型号仍明确 gemini-3.8-flash-low。

【首次编码历史记录：当前结论以上方尝试 2 为准】

▶ 首次编码结论
• 阶段 07 编码与固定回归通过，可交外层独立代码／安全复审。本会话没有启动嵌套 agent，编码者自查不冒称独立审查已通过；未越过阶段 08、真实供应商或生产发布闸门。
• 实际后端 797 passed（原 675 项全部保留，新增 122 项），前端 81 passed，E2E 5 passed（1 个总场景与 4 个连续子场景），构建通过。测试均为当前 worktree、临时数据、127.0.0.1 与假提供方。
• 已完整阅读实施计划、Spec v1.0、FastAPI 接口文档，优先核对 06-ui、06-ui-代码收尾、05-cli 记录与实际源码；按需完整读取相关 schema、路由、模型、worker、CLI 夹具与前端行为。指定旧 user_stop 仅为已被后续 06-ui 记录取代的历史暂停，不据此回滚或重建已完成阶段。

▶ 可运行闭环
• 新增 `npm --prefix frontend run test:e2e`，pretest:e2e 先构建本轮 Vue。node:test＋既有 Playwright 驱动宿主 Chrome，真实新 FastAPI 同源服务本轮 dist，不拦截或替换任何 API 响应；浏览器 route 只拒绝本轮来源以外的请求。
• Python 测试入口在 backend/var/e2e-* 新建临时 SQLite／素材／假提供方账本，独立文字和媒体 worker 子进程运行真实 RunManager／TaskWorker／ToolGateway／AgyProvider。唯一 CLI 启动器执行 Python 官方协议替身，不执行真实 agy。
• 本轮 openssl 生成一天有效的合成自签证书，先实际绑定 127.0.0.1:0 socket 再使用所获随机端口，无探端口竞争。仅临时浏览器 context 忽略证书链；Secure、HttpOnly、SameSite、__Host Cookie、Origin 和 CSRF 保持原样，不改全局证书或安全设置。
• 测试入口拒绝 worktree 之外、符号链接或复用已有 data 的目录；worker 禁止网络 connect，进程环境只传必要 PATH／Python 设置，不读取 .env 或授权。停止／重启只通过父子 stdin 控制，不增加测试 HTTP 后门或产品管理员解锁入口。
• 正常退出关闭所有本轮 context、浏览器、API、worker 及其假 CLI，随后删除临时目录。未终止其他 tmux／进程。

▶ 浏览器实际断言
• 管理员登录、邀请签发、用户邀请注册与 fragment 清除、邀请复用拒绝；第二用户独立账号。网站 Cookie 实际 secure／httpOnly／Lax、脚本不可读；缺 CSRF 写入 403，普通用户管理员入口 403。
• 新对话→合成图片上传→自然语言明确生图→真实 CLI 协议、网关与持久任务→SSE 卡片自动出现。不是刷新或合成接口产生作品。
• 选择生成图的精确版本并明确确认→文字请求首帧视频→停止媒体 worker 使任务保留 queued→第二页面读取同一事实→关闭原页面→重启 worker→重开原对话并实际下载视频。假提供方仅一次提交；requested 4 秒与合成文件实际 1.25 秒分别断言，视频浏览器元数据正常加载。
• 真正 GET 下载、HEAD 无正文及长度、Range 206／Content-Range；跨用户对话、任务、作品及 GET／HEAD／Range 均拒绝，不泄露范围大小。320／390／1100px 无整页横向溢出，发送按钮保持在视口内；此前前端键盘／弹窗／媒体查询回归全部保留。
• 同用户 A/B/A：B 仅自身两条消息，回 A 接续第三轮；实际检查两份假 CLI stdin 文件、独立 CLI ID 数据库映射，A/B 不含对方随机内容。短暂浏览器离线后恢复并接续，未重放生成。
• 对话同键同语义重放、不同语义 409；图片精修通过真实窗口提交，追加第二版本且 parent 指向原图；旧视频确认不被新版替换。任务原键重放返回相同 ID 与 Idempotency-Replayed，提供方账本仍只有一次。
• 下载失败保存 saving／can_recover，重启 worker 后只恢复原结果；提交响应丢失保存 submission_unknown、recover 409，再重启仍不重新提交。网站注销通过真实按钮，另一页旧 SSE 及时失效回登录，作品与账号访问 401。

▶ app.openapi 与设计契约差异及修复
• 新增 `backend/src/omniflow/openapi.py`：补齐已有运行时校验器未自动导出的可选非 null、目标／基础版本成对、正文或附件、首帧确认条件，以及 TaskCreate、SSE 事件／control schema、管理员角色、事件游标与响应安全头。该模块不读取／复制设计文件来代替真实路由输出；仍基于 FastAPI／Pydantic 生成结果及实际 EVENT_DATA。
• Problem 公开响应始终包含 type／retryable 并排除 errors=None，导出声明现在与实际一致；content_url 标注 uri-reference。统一 PageCursor 1–512 字符约束加入对话／消息／run／task／作品分页及快照，保持真实生成的游标规则。
• 新增 122 项全契约回归：64 个设计 schema、51 个操作、路由集合与引用／securitySchemes、6 类请求的 null／额外字段拒绝。逐操作比对请求体、所有参数、成功状态、设计错误响应、SSE 事件、Cookie／CSRF／管理员角色与响应头；HEAD 保持无正文。
• 显式等价归一化仅包括描述／标题／示例／默认元数据、$ref 与内联、排序、const 与单值 enum、冗余 enum 类型和空 required。唯一批准的语义收窄为产品文本型号固定 gemini-3.8-flash-low，而非设计能力列表的三个 Flash 候选；不能为相等放开型号。
• 统一安全边界会额外声明若干 Problem 错误状态，测试允许这些额外错误，但不允许缺少设计响应、增加成功状态或额外业务路由。未修改设计 OpenAPI、build_openapi.py 或契约测试来伪造通过。

▶ 过程失败与修复如实记录
• 首次新增契约专项 15 failed／107 passed：发现管理员／事件定义、null／依赖条件、游标、响应头等导出遗漏；比较器自身还误把对象属性名 title 当 schema 标题删除。后者已修正为保留 properties／headers／mapping 的键，并保留严格字段断言。后续依次 2 failed／120 passed、1 failed／121 passed，补齐已实际发送的 nosniff／X-Accel-Buffering 声明后，最终全量通过。
• 首次 E2E 邀请注册已通过，其他失败源于新增测试误用不存在的「Agnes 图片」标签、只在空会话显示的「新建对话」按钮及未展开手机导航；核对实际组件后修正定位，不改产品预期。
• 随后同图作为前轮产物与后轮附件出现两次，严格定位冲突；改为明确选择最早的产物卡片。再一次失败为下拉框隐式标签包含选项文本，exact 不能匹配；沿用既有布局测试的 getByLabel，并等待精修初始读取完成。没有删断言、放宽安全条件或把 API 改假来通过。
• 全量前端与后端测试后 Ruff 发现 problems.py 新类型别名前多一个空行，已精准修正并复查；仅格式变更不再次重复全部成功测试。

▶ 最终实际命令与结果
```bash
npm --prefix frontend run test:e2e
.venv/bin/python -m pytest backend/tests -q
npm --prefix frontend run test -- --run
npm --prefix frontend run format:check
.venv/bin/ruff check backend
.venv/bin/ruff format --check backend
.venv/bin/python docs/api/test_contract.py
git diff --check
git diff --exit-code -- server.py index.html requirements.txt deploy.sh \
  docker-compose.yml Dockerfile nginx.conf vercel.json start.sh manage.sh
sha256sum docs/specs/assets/codex-wave-rail.approved.html
```
• E2E 最终 5 passed／0 failed／0 skipped，11.29s；其 pretest:e2e 实际执行 `npm run build`：Vite 7.3.6、24 modules、754ms，CSS 16.17kB／JS 139.74kB。前端生产源码未改，原产物摘要名称保持不变。
• 后端最终 797 passed in 175.95s；前端最终 5 文件／81 passed in 18.84s，无跳过。原 675 后端与 81 前端检查全部保留；全量包含以前的假 CLI、worker 故障恢复、合成本地运镜及回环冒烟。
• Prettier 通过；Ruff 与格式最终通过，85 files already formatted。文档契约 40 路径／51 操作／64 schema／525 引用、22 契约示例／8 文档 JSON／1 SSE 帧／22 类拒绝检查通过。未提供官方 OpenAPI 元 schema，不冒称官方完整结构验证。
• 空白与列明旧站／部署文件差异检查通过；批准快照 SHA256 仍为 be8f15d9510bfb6a2bd937409c142fa7473876c17f901a7e495228ece1189448。没有重装依赖或修改锁文件，没有 commit／push／reset／clean／merge。
• 13:14 最终检查 Ruff／格式与旧站差异再次通过；按当前 worktree 路径检查无本轮 e2e_server／fake_agy／Vitest／Chrome／Uvicorn 残留，backend/var 下无 e2e-* 临时目录。
• 本地忽略日志：backend/var/07-backend-test.log、07-frontend-test.log、e2e-test.log；报告为交接事实来源，不要求读取 PNG。

▶ 修改文件
• 新增 backend/src/omniflow/openapi.py；修改 app.py、problems.py、conversation_models.py、artifact_models.py、task_models.py。
• 新增 backend/tests/test_runtime_contract.py、backend/tests/e2e_server.py；修改仅测试使用的 backend/tests/fake_agy.py，新增显式 e2e 场景，不改变原场景。
• 新增 frontend/e2e/workbench.test.js；修改 frontend/package.json、frontend/README.md；本报告 docs/progress/07-e2e.md。
• 未改前端产品组件／配色、真实适配器、数据库迁移、原站、部署配置、批准原型、实施计划、控制器或控制状态。全部已有未提交成果保留。

▶ 未实现／未验证与接续
• 外层独立代码／安全审查仍待执行，请重点核对测试生命周期、无 API 替身、安全 Cookie、生成账本、schema 补充是否反映实际校验器，以及严格契约比较器。通过后进入阶段 08 全量最终审查与交接，不自动发布。
• 真实 agy／Agnes、费用／免费权益与超额关闭证据、完整宿主隔离、真实质量／图生图保真、长时运行、代理 SSE、Mac／iOS 真机播放仍未验证。测试证书不等于正式证书部署，假提供方成功不等于真实模型生成验收。
• 本轮未新增生产文字 worker 就绪探针，现有 capabilities 的文字状态仍保守为 PROVIDER_UNAVAILABLE；E2E 独立注入的假文字 worker 能执行持久 run，不据此放开生产闸门或伪造真实提供方可用。
• 生产发布、生产迁移、真实供应商验证均「未完成」。未动生产库、Google 授权、收费回退或公网；产品型号仍为 gemini-3.8-flash-low。

---
▶ In brief
• What's happening：新版网页已接上真正的新服务，完整创作和恢复检查通过，等待独立复核。
• Reason：用专门造的测试内容走完整流程，确认关页面和重复操作不会多做一份。
• Impact：旧网站不变；真实生成、真机体验和正式发布仍未完成。
• Require Input：不需要 input，继续按已批准流程交接。
