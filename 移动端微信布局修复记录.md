【移动端微信布局修复记录】

▶ 用户现象
微信打开页面只能看到黑色侧栏，难以操作。未取得用户当时访问的完整 URL、截图与微信版本，不能断言是微信内核特有故障。

▶ 已核实问题
• 正式 index.html 保留固定宽 256px 的黑色导航栏，会话栏默认再展开 256px。此前只部署独立原型，未改正式首页，手机访问原首页仍会挤压内容。
• 原 HTML 的视频工作区末尾有一个多余的 div 闭合标签。新增底部导航时，浏览器会将它解析到 #app 外，导致导航不受 Vue 控制；本轮浏览器验证发现并修正。
• 视频底图 URL 输入框与两个操作并排，在窄屏出现工作区横向溢出，已改为换行布局。

▶ 实现
• 767px 及以下使用手机布局：隐藏桌面黑色导航，显示对话、文案、海报、视频底部导航。
• 历史默认关闭，改为覆盖式侧抽屉，提供关闭按钮与遮罩；新建、选中会话后收起。
• 对话标题与介绍精简，输入文字 16px，输入时隐藏底部导航。
• 使用 visualViewport 实际可视高度，兼容无该 API 时回退至 innerHeight，并处理底部安全区域。
• 工作区改为单列主体、双列选项，主要按钮最小高度 44px。
• 海报底座、布光、景别在手机上折叠为更多摄影设置，电脑默认展开。
• 未改真实生成接口逻辑与后台路由目标，未发送收费生成请求。

▶ 路由与部署
• 正式首页：https://ai.pannel.kris42y.tech/
• 正式手机版入口：https://ai.pannel.kris42y.tech/mobile
• 原独立演示迁到：https://ai.pannel.kris42y.tech/mobile-preview
• 生产部署：dpl_285mDaoTcVc242Gw1XFg1XiT6S72。
• 使用临时白名单目录发布，不上传 HANDOFF.md、.env、测试截图或其他项目文档。

▶ 验证
• HTML 标签嵌套检查与 git diff --check 通过。
• 新增 tests/mobile_layout_smoke.cjs，使用 Playwright 与本机 Chrome。
• 本地与生产 /mobile 均通过 320×568、360×640、390×844、1440×900 检查。
• 两个手机测试使用安卓微信和 iPhone 微信 User-Agent；仅模拟标识，并非真实微信内核测试。
• 验证项：导航位于 Vue 根节点内；手机隐藏侧栏；电脑保留侧栏；四区可切换且无工作区横向溢出；历史可开关；更多设置可展开；输入框不被导航遮挡；输入聚焦和失焦时导航显示正确；无页面脚本异常。
• 生产 /、/mobile、/mobile-preview 返回 HTTP 200，内容与对应本地文件逐字节一致。
• 浏览器检查拦截 /api/ 请求，不代表真实 AI 接口可用性验收。

▶ 测试执行示例

```bash
NODE_PATH=/tmp/omniflow-browser-test/node_modules node tests/mobile_layout_smoke.cjs
NODE_PATH=/tmp/omniflow-browser-test/node_modules MOBILE_TEST_URL=https://ai.pannel.kris42y.tech/mobile node tests/mobile_layout_smoke.cjs
```

Playwright 本次安装在临时目录，其他环境需自行安装并调整 NODE_PATH；默认 Chrome 路径为 /usr/bin/google-chrome，可通过 CHROME_PATH 覆盖。

▶ 尚待确认
• 用户真机微信内的键盘开合、锁屏返回与图片/视频保存尚未验证。
• 当前仍依赖外部 Vue、Tailwind 和 marked CDN，其在用户网络下的可用性未验证；本次浏览器测试加载正常。
• 若用户仍看到旧侧栏，应取得完整访问地址与截图，再区分旧页面缓存、横屏/桌面模式、资源加载失败或其他原因。
