【Codex 侧边进度条参考核对】

▶ 任务与边界
根据用户要求搜索公开参考，并用 Superpowers 建立独立交互预览来对齐“横线加波浪效果”。不是正式实现，不修改 index.html、后端或线上部署；不调用生成模型。

▶ 搜索过程
通过 HTTP 查询 DuckDuckGo、Bing，并使用 GitHub 公共搜索 API 检索 Codex 的 scrollbar、minimap、timeline 等关键词。DuckDuckGo 首轮成功、后续查询返回挑战页；Bing 结果较泛，未作为细节证据。最后通过 GitHub 的 minimap 查询找到带截图的公开反馈。

▶ 主要参考（已读取原文并查看图片）
来源：https://github.com/openai/codex/issues/38631
标题：Codex App: add message bookmarks to the existing conversation minimap。
公开反馈发表于 2026-08-14；作者描述既有会话 minimap / scroll-position rail 可在悬停时显示消息预览。
原图一：https://github.com/user-attachments/assets/25b3fc0e-5554-477d-940e-cee351fbc857
原图二：https://github.com/user-attachments/assets/7e4a778a-0a7f-45d5-819c-780e25ec0915
这是一位用户上传至官方仓库的界面截图，不是官方设计规格，也不能代表每个平台和版本。反馈中的“新增书签”是请求，不是已存在的产品能力，本次不加入书签功能。

▶ 可直接观察的外观
• 短横线竖向排列，位于对话内容左侧；不是右侧浏览器原生滚动条。
• 静止截图中横线很短、浅灰，局部有较深色标记。
• 悬停截图中横线左端大体对齐，局部横线逐渐增长，再逐渐缩短，组合成侧向波峰；横线本身不是弯曲的正弦线。
• 波峰中央横线更深，附近出现消息预览卡片。
原图一的红色标记来自反馈作者，不是产品界面。

▶ 辅助参考
https://github.com/openai/codex/issues/35071
公开反馈描述旧版对话左侧时间线，可按提问／轮次点击跳转，并提到版本升级后的差异。
https://github.com/openai/codex/issues/35080
公开反馈描述 macOS 旧会话内可拖动／滚动的历史导航在升级后消失。
这些是用户反馈，作为交互线索，不作为所有版本均有此表现的保证。

▶ 原版核实边界与用户验收
未找到并观看可确认此控件完整动态过程的录屏；没有官方动画参数或原始实现。不能声称与 Codex 原版像素级或逐帧一致。
用户查看当前 Superpowers 原型后明确表示：“yes exactly!就按照这个来”。当前原型的横线外观、波峰跟随及定位交互已获得设计确认；后续正式实现以此版本为基准，无须再等待原版录屏或重复确认该方向。官方参数尚未核实，不妨碍将用户认可的原型作为项目自身验收依据。
保留 codex-wave-rail.html 当前版本，不覆盖修改；后续如需变体，另存新文件。当前确认不代表正式功能已经接入，也不代表授权发布整个重构。

▶ 本轮预览范围
独立冷白蓝灰对话页面，左侧短横线；邻近指针的横线平滑增长、变深，组成跟随指针的局部波峰；离开后收回。支持消息预览、点击／拖动定位、滚动时活动标记更新与键盘操作。波峰动画和交互映射是根据截图与反馈制作的对齐原型，不冒充原版实现。
页面提供“参考依据”对话框，直接展示两张原截图与来源链接，明确区分已观察外观与待核实动效。
原型文件：.superpowers/brainstorm/3311356-1788847273/content/codex-wave-rail.html。
服务监听：10.172.246.153:58332，完整含访问密钥的链接仅在会话中提供，不写入可提交报告。
预览素材只含虚构创作对话和公开参考截图，不复制旧会话凭据或客户资料。使用服务器 10.172.246.153 的受会话密钥保护预览，不建立公网隧道。

▶ 本轮验证
运行 node .superpowers/scroll-preview-tests/smoke.cjs，全项通过：
• 桌面 1440×960：短横线数量、局部波峰渐变、随指针移动、离开后收回、点击跳转、拖动跳转、键盘 Home／End／方向键、参考弹窗与两张图片加载、追加预览消息时增加标记、侧栏折叠、减少动态效果偏好。
• 手机 390px 与 320px 宽度：无横向溢出、触摸定位、输入区域可见。
• 桌面浏览器未捕获页面脚本异常，未请求预览服务器以外地址；手机未捕获脚本异常。
• 已实际查看桌面悬停与 390px 手机截图；截图保存在 .superpowers/scroll-preview-tests/。
• git diff --check 通过；58332 端口确认为监听状态。
以上使用服务器 Chrome 自动化验证，不等于 Mac 浏览器或 iOS 真机验收，也不等于与 Codex 原版动画逐帧一致。

▶ 变更范围
只新增独立预览、测试与记录；.gitignore 加入 .superpowers/，防止预览访问密钥被提交。未修改正式前后端代码、未 commit／push／发布，未干扰原主工作区的 55736 预览服务。
