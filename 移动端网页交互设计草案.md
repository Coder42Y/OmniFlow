【移动端网页交互设计草案】

状态：用户已确认聊天型首页、完整创作，以及 iPhone、安卓与微信内打开。首轮独立原型保存于 prototypes/mobile.html，线上入口已迁到 /mobile-preview。根据微信布局反馈，正式 index.html 已增加手机布局并部署，/ 与 /mobile 均可访问。本地和生产已通过 Chrome 手机尺寸与微信标识模拟检查，尚未进行微信真机验收。后端未修改，详见 移动端微信布局修复记录.md。
依据：HANDOFF.md 最新版本，以及此前对 index.html 导航、布局和会话存储的局部检查。线上可用性尚未实测。

▶ 产品定位
同一域名下提供手机专属布局，复用现有业务能力，电脑端保持原有体验。不是简单缩小电脑页面，不新增原生 App 或小程序。

▶ 导航建议
底部设对话、文案、海报、视频四个入口，默认进入对话。对话页顶部提供历史会话、新建对话。第一版不新增素材库，避免暗含尚未实现的跨设备作品存储承诺。

▶ 对话页
顶部：历史入口、OmniFlow、新建对话。
空状态：简短引导与最多三个快捷需求，避免大面积介绍占屏。
正文：单列消息，图片限宽、表格局部横向滚动，保留现有结果操作。
底部：多行输入框与发送按钮；键盘打开时隐藏底部导航，优先展示输入和最近消息。输入框随可视区域调整，需真机验证。
历史：覆盖式抽屉，不挤压正文。手机和电脑目前分别保存会话，不承诺同步。
生成状态：明确区分等待、输出、完成、失败；只在实际收到结果后呈现对应成品，不暗示每轮聊天都会自动生成图片和视频。

▶ 创作工作区
采用“填写 → 生成 → 查看结果”的共同模式，避免电脑端左右分栏。
文案：关键输入优先，高级设置折叠；结果支持现有复制与后续流转操作。
海报：画幅与风格优先，复杂参数折叠；生成后展示大图及保存、送去制作视频等现有操作。
视频：底图、运镜等基础设置优先；结果竖屏预览。任务状态没有真实进度时只显示处理中，不虚构百分比。
生成按钮就近置于表单末尾，长表单是否增加底部固定操作区待原型验证，避免与导航堆叠。
切换工作区时保留已填写内容；刷新恢复与后台持续任务不默认纳入。

▶ 视觉规范建议
沿用暖白底、深色正文、琥珀色强调；减少说明性长文与技术信息。
正文建议 16px，次要信息不小于 12px，主要点击区域至少 44×44px。
遵循手机底部安全区域，控制动画，尊重减少动态效果的系统设置。
下载在 iPhone、安卓与微信内表现可能不同；以实测结果提供保存或浏览器打开引导，不承诺一键写入相册。

▶ 首版范围
保留四个工作区、历史会话抽屉、手机表单及结果展示、软键盘适配。
不默认新增登录、跨设备同步、素材中心、语音输入、后台任务系统或后端云端迁移。

▶ 待用户确认的关键方向
• 已确认：采用聊天型首页，默认进入对话，底部提供四个工作区入口。
• 已确认：以手机完成完整创作为主要场景，覆盖文案、海报、视频及成品保存；复杂参数折叠，不以轻量查看为目标。
• 已确认：覆盖 iPhone、安卓及微信内打开。建议验收组合为 iPhone Safari、安卓 Chrome、iPhone 微信和安卓微信；具体设备与版本在测试阶段记录，不承诺覆盖所有浏览器版本。
• 图片和视频保存按环境提供可用方式；微信内受限时提供在系统浏览器打开的引导。不默认承诺一键保存到相册或锁屏后继续生成。

▶ 下一步
确认首页方向后，先做手机首页与一个创作工作区的可预览原型，再确认视觉和操作细节，随后扩展其余页面并进行真机验收。
